const HttpError = require("./ApiHandleError");
const handleMongooseError = require("./handleMongooseError");
const Error = require("./Error");

module.exports = {
  HttpError,
  handleMongooseError,
  Error,
};
